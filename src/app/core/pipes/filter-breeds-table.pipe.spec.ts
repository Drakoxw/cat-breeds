import { FilterBreedsTablePipe } from './filter-breeds-table.pipe';

describe('FilterBreedsTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBreedsTablePipe();
    expect(pipe).toBeTruthy();
  });
});
