import { FilterBreedsGraphqlTablePipe } from './filter-breeds-graphql-table.pipe';

describe('FilterBreedsGraphqlTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBreedsGraphqlTablePipe();
    expect(pipe).toBeTruthy();
  });
});
